<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Update Syllabus Entry</title>
</head>
<body>
    <h1>Update Syllabus Entry</h1>

    <% if (error) { %>
        <div class="error"><%= error %></div>
    <% } %>

    <form action="/syllabus/update/<%= syllabusEntry._id %>" method="POST">
        <label>Regulation Code:</label>
        <input type="text" name="regulation" value="<%= syllabusEntry.regulation %>" required><br>

        <label>Topic Name:</label>
        <input type="text" name="topicName" value="<%= syllabusEntry.topicName %>" required><br>

        <label>Date Covered:</label>
        <input type="date" name="dateCovered" value="<%= syllabusEntry.dateCovered ? syllabusEntry.dateCovered.toISOString().split('T')[0] : '' %>" required><br>

        <label>Time Spent (in hours):</label>
        <input type="number" name="timeSpent" value="<%= syllabusEntry.timeSpent %>" required><br>

        <button type="submit">Update Topic</button>
    </form>

    <a href="/syllabus/table">Back to Syllabus Table</a>
</body>
</html>
